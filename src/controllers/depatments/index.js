const createOne = require('./create-one');
const changeOne = require('./change-one');
const getAll = require('./get-all');

module.exports = {
  createOne: createOne,
  changeOne: changeOne,
  getAll: getAll
};